import { styled, Typography } from '@mui/material';

const ZuTypography = styled(Typography)(({ variant, theme }) => ({
  // Your custom styles here
  fontSize: '14px',
  color: 'white',
  fontFamily: 'Inter'
}));

export default ZuTypography;
